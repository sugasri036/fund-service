apiVersion: v1
kind: Service
metadata:
  name: fund-service
  namespace: investra
spec:
  selector:
    app: fund-service

  type: ClusterIP

  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081